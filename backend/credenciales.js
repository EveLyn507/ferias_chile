// config.js
const user_name = 'postgres'; // Cambia por tu nombre de usuario
const host_name = 'localhost'; // Cambia por el nombre de host si es diferente
const database_name = 'feria'; // Cambia por el nombre de tu base de datos
const pass = 'admin123'; // Cambia por tu contraseña
const puerto = 5432; // Puerto de PostgreSQL

module.exports = {
  user_name,
  host_name,
  database_name,
  pass,
  puerto,
};
